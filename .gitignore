node_modules/

# Build output: the pages vite and ssg write, and the zip deploy.sh ships.
/dist
/build
build.zip

# What content/blog.ts writes from content/blog/: the index, the twins and the tokens, the media
# under its hashed names, and the two site images it renders. The sources are in content/.
/frontend/data/posts.json
/frontend/public/blog/
/frontend/public/media/
/frontend/public/site-card.png
/frontend/public/profile.dark.png

# The soundfont npm run soundfont fetches; 32 MB, shipped in the zip, never committed.
/radio/assets/

# Screenshots the proof program takes.
/proof-shots

.env
*.log
.DS_Store
