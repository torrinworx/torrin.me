node_modules/

# Build output: the pages vite and ssg write, and the zip deploy.sh ships.
/dist
/build
build.zip

# Screenshots the proof program takes.
/proof-shots

# The aweft stack, cloned beside this project rather than committed or vendored. Until it has a
# git remote there is nothing to point a submodule at, so a fresh checkout clones it by hand.
# See README.md.
/aweft

.env
*.log
.DS_Store
