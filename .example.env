ENV=development
PORT=3000
# The radio's own port; nginx proxies /radio/stream to it. 3010 when unset.
RADIO_PORT=3010
RESEND_API=
EMAIL_FROM=
EMAIL_TO=
# Postgres for the logs battery's visits: the site's own database on the droplet.
db=postgresql://torrinme:PASSWORD@127.0.0.1:5432/torrinme
