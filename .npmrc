# The stack lives in the aweft submodule as TypeScript source. Its packages point their exports at
# the compiled dist/ a published package carries, which a submodule checkout does not have, so
# every script npm runs here asks for the source instead (aweft design 256).
node-options=--conditions=aweft-source
